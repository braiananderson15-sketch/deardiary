#!/usr/bin/env node
// deardiary CLI — entry point.
// Commands: log, read, context, stats, random, export, mcp,
//           setup (+ --check), doctor, bench-startup, uninstall.
console.error("deardiary: not implemented yet — scaffold only. See DESIGN.md.");
process.exit(1);
